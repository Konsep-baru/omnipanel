🚀 QUICKSTART OMNIPANEL V1.0 - VERSI SIMPEL

⚡ MULAI DALAM 5 MENIT

---

📥 1. INSTALL

```bash
# Download installer
wget -O omnipanel-install.sh https://raw.githubusercontent.com/username/omnipanel/main/install.sh

# Install (sebagai root)
sudo bash omnipanel-install.sh install
```

Saat install:

· Anda akan diminta ** memasukkan password** untuk user omnipanel
· Password minimal 6 karakter
· Simpan password ini!

---

🔐 2. LOGIN KE PANEL

```bash
# Ganti 192.168.1.100 dengan IP server Anda
ssh -p 4086 omnipanel@192.168.1.100
Password: (password yang Anda buat tadi)
```

Jika berhasil login, Anda akan melihat:

```
╔════════════════════════════════════════╗
║         OMNIPANEL V1.0                 ║
║     Docker Management System           ║
║     Type 'help' for commands           ║
╚════════════════════════════════════════╝

omni>
```

---

🐳 3. COBA PERINTAH PERTAMA

```bash
# Lihat semua perintah yang tersedia
omni> help

# Pull image nginx
omni> image pull nginx:alpine

# Jalankan container nginx
omni> container run nginx:alpine
Container name: web
Port (e.g., 8080:80): 8080:80
Run in background? [Y/n]: y
✓ Container created

# Lihat container yang running
omni> container ls

# Lihat DNS entries
omni> dns ls
🌐 DNS ENTRIES (.lan):
192.168.1.100   panel.lan
192.168.1.100   web.lan
```

---

🌐 4. AKSES WEBSITE

Dari browser di laptop/PC Anda:

```
http://192.168.1.100:8080
```

Atau kalau sudah setting DNS di client:

```
http://web.lan:8080
```

---

📋 5. COBA PERINTAH LAINNYA

```bash
# Lihat log container
omni> container logs web

# Masuk ke dalam container
omni> container exec web bash
root@web:/# ls /usr/share/nginx/html/
root@web:/# exit

# Lihat statistik container
omni> container stats

# Stop container
omni> container stop web

# Hapus container
omni> container rm web
```

---

🚪 6. KELUAR DARI PANEL

```bash
omni> exit
Goodbye!
Connection closed.
```

---

🎯 RINGKASAN PERINTAH DASAR

Perintah Fungsi
help Lihat semua perintah
image pull nginx Download image
container run nginx Jalankan container
container ls Lihat container running
container logs web Lihat log
container exec web bash Masuk ke container
container stop web Stop container
dns ls Lihat DNS entries
version Lihat versi
exit Keluar panel

---

⚠️ TROUBLESHOOTING CEPAT

*Gagal akses .lan?

```bash
# Akses via IP saja
http://192.168.1.100:8080
```

Lupa password?

```bash
sudo ./omnipanel-install.sh password
```

Docker tidak ditemukan?

```bash
# Install manual
curl -fsSL https://get.docker.com | sudo sh
```

---

✅ YANG SUDAH ANDA CAPAI

· Install OmniPanel
· Login via SSH
· Pull image nginx
· Run container
· Akses website via browser
· Lihat log container
· Masuk ke dalam container
· Stop container

Selamat! OmniPanel V1.0 sudah siap digunakan! 🚀
